# ARTEMIS2 - Get descriptions

We cloned ShareGPT4Video GitHub code and modified the code to be used for our case. You can
get the description from folders of images using the `run.py` file inside the folder. Otherwise,
you can already use the video description available in `video_descriptions` for each of the three
dataset used in the paper.